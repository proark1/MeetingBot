"""Browser-bot submodules.

Extracted from the monolithic ``browser_bot.py`` to make the platform-specific
data and helpers easier to find and edit. ``browser_bot.py`` re-imports these so
all existing ``browser_bot.X`` references and external imports keep working.
"""
