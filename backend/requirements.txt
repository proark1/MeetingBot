fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
pydantic==2.10.3
pydantic-settings==2.6.1
httpx==0.28.1
python-multipart==0.0.20
aiofiles==24.1.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.1
aiosqlite==0.20.0
greenlet==3.1.1

# AI — Claude (Anthropic) for analysis when ANTHROPIC_API_KEY is set
anthropic>=0.50.0

# AI — Gemini for transcription and analysis when GEMINI_API_KEY is set
google-generativeai==0.8.3

# Gemini Live API (real-time bidirectional audio streaming)
# New unified SDK — coexists with google-generativeai, different import path
google-genai>=1.0.0

# Real browser bot
playwright==1.49.1
playwright-stealth==1.0.6

# Text-to-speech for voice responses
edge-tts>=6.1.9

# Scheduled background jobs (weekly digest, recording cleanup)
apscheduler>=3.10
