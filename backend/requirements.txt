fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.1
email-validator==2.3.0
httpx==0.28.1
python-multipart==0.0.32
aiofiles==25.1.0
python-dotenv==1.2.2

# AI — Claude (Anthropic) for analysis when ANTHROPIC_API_KEY is set
anthropic==0.107.1

# AI — Gemini for transcription and analysis when GEMINI_API_KEY is set
google-generativeai==0.8.6

# Gemini Live API (real-time bidirectional audio streaming)
google-genai==2.8.0

# Real browser bot
playwright==1.60.0
playwright-stealth==2.0.3

# Text-to-speech for voice responses
edge-tts==7.2.8

# PDF export
reportlab==4.5.1

# ── Multi-tenant billing ──────────────────────────────────────────────────────

# Database (async SQLAlchemy + SQLite driver for dev, asyncpg for PostgreSQL/Railway)
SQLAlchemy[asyncio]==2.0.50
aiosqlite==0.22.1
asyncpg==0.31.0

# Auth (password hashing + JWT)
bcrypt==5.0.0
python-jose[cryptography]==3.5.0

# Jinja2 templates for web UI (usually included with fastapi[all], listed explicitly)
jinja2==3.1.6

# Stripe card payments
stripe==15.2.0

# Rate limiting
slowapi==0.1.9

# Cloud storage (S3-compatible: AWS S3, Cloudflare R2, MinIO)
boto3==1.43.24

# Calendar auto-join (iCal feed parsing)
icalendar==7.1.2
recurring-ical-events==3.8.2

# Email notifications via SendGrid (optional — SMTP uses stdlib)
sendgrid==6.12.5

# Prometheus metrics (optional — graceful degradation if not installed)
prometheus-client==0.25.0

# Shared bot-state backend (used only when BOT_STATE_BACKEND=redis; the default
# in-memory store needs no Redis). redis.asyncio ships with this package.
redis==8.0.0

# SSO — Google & Microsoft OAuth2 (httpx is pinned once near the top of this file)
authlib==1.7.2

# USDC/ERC-20 crypto payments — installed separately (see requirements-crypto.txt)
# web3 and eth-account are large packages; they are installed in a dedicated Docker
# layer so a build timeout does not break the rest of the application.

# ── Testing ──────────────────────────────────────────────────────────────────
pytest==9.0.3
pytest-asyncio==1.4.0
pytest-timeout==2.4.0
fakeredis==2.36.1   # in-memory Redis for RedisBotStateStore tests (no real Redis needed)

# Error tracking (optional — graceful no-op if SENTRY_DSN unset)
sentry-sdk[fastapi]==2.61.1

# Schema migrations (Alembic). The legacy in-app _migrate_schema in db.py still
# runs by default; Alembic is the forward path for new schema changes.
alembic==1.18.4
